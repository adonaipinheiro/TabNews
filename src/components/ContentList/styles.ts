import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    gap: 10,
    flex: 1,
  },
  flatList: {
    flex: 1,
    padding: 10,
  },
});

export default styles;
